#include <iostrem>
#inlcude <map>
#include <vestor>

class LearnMap{
public :
    LearnMap();
    ~LearnMap();
private:
    void create_dict(void);
    void print_map(void);
};

int main(){
	return 0;
}

